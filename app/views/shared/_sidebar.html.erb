<aside class="w-64 bg-surface border-r border-stroke-secondary flex flex-col">
  <nav class="flex-1 p-4 space-y-1">
    <%= sidebar_link_to "Home", root_path, icon: "home" %>
    <%# Add more navigation items here %>
  </nav>
  <div class="p-4 border-t border-stroke-secondary">
    <div class="flex items-center gap-3">
      <div class="w-8 h-8 rounded-full bg-stroke-secondary flex items-center justify-center">
        <span class="text-sm font-medium text-gray-600">
          <%= Current.user&.email_address&.first&.upcase %>
        </span>
      </div>
      <div class="flex-1 min-w-0">
        <p class="text-sm font-medium text-gray-900 truncate">
          <%= Current.user&.email_address %>
        </p>
      </div>
    </div>
  </div>
</aside>
